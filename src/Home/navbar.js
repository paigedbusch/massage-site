import React, { Component } from "react";

class NavBar extends Component{

    render() {
        return(
            <div>
                links and stuff
            </div>
        )
    }
}

export default NavBar;